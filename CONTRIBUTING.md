# Contributing to USB Composite Library

Спасибо за интерес к проекту! 🎉

## Как внести вклад

### Сообщение об ошибках

1. Проверьте [Issues](../../issues) — возможно, ошибка уже известна
2. Создайте новый Issue с описанием:
   - Версия библиотеки
   - Модель микроконтроллера (STM32H743, H750, etc.)
   - Минимальный воспроизводимый пример
   - Ожидаемое и фактическое поведение

### Pull Requests

1. Fork репозитория
2. Создайте feature branch: `git checkout -b feature/my-feature`
3. Внесите изменения
4. Убедитесь что код компилируется без ошибок
5. Создайте Pull Request

### Стиль кода

- **C++**: C++17, snake_case для функций/переменных, PascalCase для классов
- **Отступы**: 4 пробела
- **Комментарии**: Doxygen-style для публичного API
- **Строки**: max 100 символов

### Тестирование

Перед отправкой PR убедитесь:

```bash
# Компиляция без ошибок
pio run -e stm32h743

# Проверка на разных конфигурациях
pio run -e stm32h743 -D USB_CDC_ENABLED
pio run -e stm32h743 -D USB_MSC_ENABLED
pio run -e stm32h743 -D USB_CDC_ENABLED -D USB_MSC_ENABLED
```

## Roadmap

- [ ] Поддержка STM32F4
- [ ] HID класс (клавиатура/мышь)
- [ ] RTOS интеграция
- [ ] Примеры для CubeIDE

## Контакты

- Issues: GitHub Issues
- Email: [указать email]

---

Все вклады приветствуются! 💪
